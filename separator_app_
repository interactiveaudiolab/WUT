server {
    listen      80;
    server_name http://localhost:5000/;

    location / {
        include proxy_params;
        proxy_pass http://unix:/deploy/separator_app/unix:separator_app.sock;
    }
}